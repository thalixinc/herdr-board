[package]
name = "herdr-board"
version = "0.1.0"
edition = "2021"
description = "Greenfield herdr kanban plugin: GitHub-issue board + card-to-factory trigger"
license = "UNLICENSED"

# Dependencies land with epic #8, gated on the herdr 0.9.0 plugin/tab/pane
# compatibility qualification (REQUIREMENTS.md, critic repair G1).
[dependencies]
